{
  "$comment": "lazy2nix settings. Excluded plugins keep being managed by lazy.nvim because they have a build step or are local development plugins.",
  "exclude": [
    "firenvim",
    "markdown-preview.nvim",
    "mason.nvim",
    "nvim-in-the-loop",
    "nvim-treesitter"
  ],
  "overrides": {}
}
