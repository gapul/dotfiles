services:
  navidrome:
    image: deluan/navidrome:latest
    container_name: navidrome
    environment: [ND_SCANSCHEDULE=1h, ND_LOGLEVEL=info, TZ=Asia/Tokyo]
    volumes:
      - ./data:/data
      - /mnt/jellyfin-media/music:/music:ro
    ports: ["4533:4533"]
    restart: unless-stopped
