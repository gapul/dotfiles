# 全ツール統一テーマ Rosé Pine。flavor は read-only store のため vendor。
# 更新: Mintass/rose-pine.yazi を再取得して flavors/ に再コピー。
# dawn flavor は rose-pine.yazi を dark→dawn の hex 置換で生成 (theme.nix の 2 端点に対応)。
# yazi はターミナルの外観に応じて dark/light flavor を自動選択 → macOS 外観に追従。
[flavor]
dark = "rose-pine"
light = "rose-pine-dawn"

# ── 半透明化 ──
# flavor は overall 背景を不透明な base 色 (#191724 等) で塗るため ghostty の
# background-opacity / blur が yazi 内で潰れる。theme.toml は flavor の上に
# マージされるので、全体とステータスの背景だけ "reset" (端末デフォルト=透過) で
# 上書きし、配色はそのまま端末の半透明を透かす。
[app]
overall = { bg = "reset" }

[status]
overall = { bg = "reset" }
