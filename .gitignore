# macOS
.DS_Store

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Nix
result
result-*
.direnv/

# home-manager backups
*.backup
*.before-nix
*.before-nix-darwin

# Logs / tmp
*.log
*.tmp
tmp/

# バックアップ残骸 (clean-junk 対象。コミットしない)
*.bak
*.bak.*
*.orig

# ビルド成果物: sketchybar helper は helper.c + makefile から make で生成する
configs/wm/sketchybar/helper/helper

# karabiner 自動バックアップ (アプリ生成)
configs/keyboard/karabiner/automatic_backups/
configs/wallpaper/state.js
configs/cli/calcurse/caldav/config
configs/ssh/config

# git-hooks.nix が生成 (nix develop で再生成・store パス依存のため非追跡)
.pre-commit-config.yaml

# Claude Code が生成 (ScheduleWakeup のロック等)
.claude/

# Lab PC 用の OS-username override (個人情報、commit 禁止)
nix/user.local.nix
